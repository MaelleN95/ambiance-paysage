<footer>
    <div class="max-width-container">
        <div class="address-hours">
            <img src="{{ asset('images/logos/logo-footer.svg') }}" alt="Logo Ambiance Paysage" class="footer-logo">
            <p>23 impasse des Cigales<br>
            13124 Peypin<p>
            <ul>
                {% for schedule in schedules %}
                    <li>
                        <span class="day-name">
                            {{ schedule.dayName|trans }}
                        </span>

                        {% if schedule.mode == 'closed' %}
                            <span>Fermé</span>

                        {% elseif schedule.mode == 'open' %}
                            <span>
                                {{ schedule.morningStart|date('H:i') }}
                                -
                                {{ schedule.afternoonEnd|date('H:i') }}
                            </span>

                        {% elseif schedule.mode == 'break' %}
                            <span>
                                {{ schedule.morningStart|date('H:i') }}
                                -
                                {{ schedule.morningEnd|date('H:i') }}
                                /
                                {{ schedule.afternoonStart|date('H:i') }}
                                -
                                {{ schedule.afternoonEnd|date('H:i') }}
                            </span>
                        {% endif %}
                    </li>
                {% endfor %}
            </ul>

        </div>
        <div class="text-content">
            <div class="social-networks">
                <h3>Retrouvez-moi sur les réseaux sociaux</h3>
                {% for socialNetwork in socialNetworks %}
                {% if socialNetwork.isVisible %}
                <div class="one-social-network">
                    <a href="{{ socialNetwork.link }}" target="_blank" rel="noopener" title="Lien vers {{ socialNetwork.name }}">
                        {{ socialNetwork.icon|raw }}
                    </a>
                    <span class="social-network-title">{{ socialNetwork.title }}</span>
                </div>
                {% endif %}
                {% endfor %}
            </div>
            <div class="page-links">
                <h3>Liens utiles</h3>
                <a href="{{ path('app_homepage') }}">Page d'accueil</a>
                <a href="{{ path('app_gallery') }}">Galerie de mes réalisations</a>
                <a href="{{ path('app_contact') }}" data-turbo="false">Me contacter</a>
                <a href="{{ path('app_legal_notice') }}">Mentions légales</a>
            </div>
        </div>
    <div>
</footer>